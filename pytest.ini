[pytest]
# [SAFETAIL][AUDIT] Regression suite (plan.md 11). One test per fixed defect,
# named test_<ID>_<slug>.
testpaths = tools/tests
addopts = -q -ra -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning
