# Changelog

Newest first. One row per merged change. Format (plan.md §10.6):

`date · ID · files · what · why · gate · changes published numbers? (Y/N)`

A `Y` row must link to a before/after table (kept under `results/` or `figures/`).

Convention: **ID** is the defect (`D-xx`), missing-feature (`M-xx`), spec-defect
(`S-xx`), workstream (`B0`…`B12`, `C-*`), or v1-port (`V1-BUG-xx`, `V1-DEV-xx`)
identifier from `plan.md`. The same ID must also appear as a `[SAFETAIL][…][ID]`
tag at the code site and in the regression test name.

---

## Unreleased

> **Note (2026-09-07).** Rows below stop at `B9`. Commits `e744724` (B5),
> `d2a7397`, `6b95750`, `e085dfa`, `6eb680f`, `2fc7e56`, `5d109d6`, `a174166`,
> `98dcda4` and `fd6cc64` landed *after* the last row was written, so this table
> under-reported the work by ten commits. The four rows dated 2026-09-03 and
> 2026-09-07 below close that gap. Authoritative narrative:
> `FIXES_REPORT_SafeTail2.0_to_heterogenous.md` at the project root.

| date | ID | files | what | why | gate | Δ numbers |
|---|---|---|---|---|---|---|
| 2026-09-08 | verification pass 3 | `tools/check_manifest.py`, `tools/tests/test_followup_fixes.py` | `--dir` results drove G5's verdict and exit code correctly but never entered the `good`/`bad` lists, so the summary line counted them as neither — a failing `--dir` run printed `0 failing` while exiting 1, and (visible only once the tree was clean) a passing one printed `0 ok` while exiting 0. Both directions now counted, with a regression test. Raised twice by the Claude Code verification session; the second time from the first provenanced run in the project's history. | a tally that disagrees with its own verdict teaches people to ignore the tally | G5 | **N** — reporting only |
| 2026-09-08 | verification pass 2 | `tools/check_manifest.py`, `tools/verify_dataset.py`, `plan.md` §11, `HANDOFF_CLAUDE_CODE.md`, `FIXES_REPORT_*.md` | Second round of Claude Code findings on the settled tree (**48 passed / 0 failed**, G0–G7 all PASS, clean smoke run; G2 now reports 435 distinct transition rewards vs 431, consistent with server 5 going stochastic under `TRACE_SAMPLING=sample`). Three fixes: `check_manifest.py --dir` printed `0 failing` while exiting 1 — the `--dir` path set a bool but never entered `bad`, so the tally line lied (now counted); `verify_dataset.py` printed `[FAIL]` while exiting 0, which would silently pass unusable data when chained with `&&` — the marker now matches the exit code (`[INADEQUATE]`/0 advisory, `[FAIL]`/1 under `--strict`), with the advisory contract stated in the output; **G8 registered in plan.md §11** (was documented only in a CHANGELOG row). | a gate whose severity marker disagrees with its exit code is worse than no gate | G5, G8 | **N** — reporting and exit codes only |
| 2026-09-08 | verification feedback | `src/_manifest.py`, `tools/check_manifest.py`, `src/constants.py`, `FIXES_REPORT_*.md`, `HANDOFF_CLAUDE_CODE.md` | Applied all findings from the Claude Code verification session (`HANDOFF_FROM_CLAUDE_CODE.md`), which ran the suite on the real machine: **45 passed / 0 failed, all 7 gates PASS, clean smoke run** — closing the TensorFlow-blocked gap, and confirming at runtime that D-32's access-rate series is genuinely per-episode and D-38's P(T) is not inverted. Fixes: `_manifest.py` recorded `HANGELOG.md` because `_git()` strips the first porcelain line's leading space and `l[3:]` then ate a character (now `l[2:].strip()`); `check_manifest.py` gained `--dir` so out-of-tree runs (`--smoke` writes to `tools/out/`) can be gated at all; `no_of_episodes` derivation no longer hardcodes `4` for `max_burst`; report corrected on the '35 pass / 10 blocked' arithmetic (19, not 10) and on `server5.csv` being **369/363**, not 363/363. | a manifest exists to get provenance right, and a gate that cannot see a run cannot gate it | G5 | **N** — provenance and docs only |
| 2026-09-08 | **D-40b** (D-40 plumbing) + **G8** | `src/regressors.py`, `src/servers.py`, `src/constants.py`, `tools/verify_dataset.py` (new), `NEW_DATASET_CHECKLIST.md` (new), `tools/tests/test_followup_fixes.py` | Prepared the code for the incoming distributional dataset. **Two traps removed.** (1) `TracePredictor._row` was an unconditional `.iloc[0]`: repeated measurements would have been silently discarded, so the new data would have changed nothing and looked like it achieved nothing. Now `constants.TRACE_SAMPLING` (`sample`/`first`/`mean`), with `LEGACY_ENV` pinning `first` so `results/reference_v0` still reproduces exactly. Same `.iloc[0]` fixed in `servers._csv_lookup`. (2) **The regressors are point predictors fitted on squared error — they return E[y|x], the conditional MEAN**, so repeats get averaged away regardless. Proof from the only repeats the current data contains (server5 `'sd'`): measured 33.7991 and 23.5592 ms, RandomForest returns 20.9059 for BOTH. Added `constants.COMPUTATION_SOURCE='empirical'` — sample the MEASURED values, fall back to the model only for unmeasured contention strings. New gate **G8** `tools/verify_dataset.py` audits a dataset for repeats, spread (CV), coverage of all 34 contention strings, and schema before anyone trains on it. | collecting repeats is necessary but NOT sufficient; without both halves the data collection cannot produce a tail (D-40) | G8 (new) | **N** — both defaults preserve current behaviour (`COMPUTATION_SOURCE=model`; `TRACE_SAMPLING=sample` is a no-op except on server5's 6 accidental repeats, and legacy mode pins `first`) |
| 2026-09-08 | dossier pt.2 / **D-42 D-43 D-44** D-45 D-15(ext) | `src/main.py`, `src/constants.py`, `src/controller.py`, `src/user.py`, `tools/make_figures.py`, `plan.md` §4.4b, `tools/tests/test_followup_fixes.py` | From dossier pages 9–20 (the first copy was truncated at page 8). **D-42 FIXED:** the documented socket entry point could **never terminate** — `training_done.wait()` had no timeout and its target (`no_of_episodes` = 25,000) exceeded what the sender can deliver (1000×4 chunks → 1,333 episodes) by ~19×; now a bounded `SAFETAIL_DRAIN_TIMEOUT` wait that finalises. *(Runs under `results/` used the in-process path and were unaffected.)* **D-43 FIXED:** episode length (3 chunks) and episode count (÷4) disagreed — one `constants.CHUNKS_PER_EPISODE` now drives both, clamped to deliverable chunks. **D-44 FIXED:** `Request.__init__` accepted `deadline` and discarded it. **D-45 documented:** `latency_log.csv` mixes seconds (components) and ms (totals) with no unit in the header; `make_figures` compensated with a bare `*1000.0` — now an explicit `_TO_MS` map, columns deliberately not renamed. **D-15 extended:** server 2's models use the CPU schema while its CSV is a GPU trace, and its `detect` pickle is byte-identical to server 4's — the real Ryzen 5 7600X trace is absent. Socket path also writes a G5 manifest now. | the shipped entry point could not finish a run; the dossier's β=4 `ACTIVE_SERVERS` resolution of server 2 differs from this branch's alias-to-1 and the two must not be mixed | G5 | **YES for D-44** — adds 2 elements to the state vector (`request_to_state_array` flattens every attribute, D-35). Native 2.0 only; rides along with the D-38 re-run. D-42/D-43 change termination, not physics. |
| 2026-09-08 | dossier / **D-38** D-39 D-40 D-41 | `src/controller.py`, `plan.md` §4.4b, `tools/tests/test_followup_fixes.py` | Four findings from the external *SafeTail Defect Dossier* (Uttam, 3 Sep 2026), each reproduced here first. **D-38 FIXED:** `P_T = (T-D1)/(D2-D1)` was missing its leading `1 -`, so degree-of-satisfaction *increased* with lateness (T=40 ms scored 0.06, should be 0.94) and the piecewise was discontinuous at **both** deadline boundaries; `P(T)` feeds ω in the **episodic** reward. **D-39** (four predictors fed unscaled features to scale-sensitive linear models, RMSE ten orders of magnitude out) was already fixed here as a side effect of B1's wrapper unification — now registered and tested. **D-40 OPEN:** computation latency has *zero* variance (traces: 363 rows / 363 unique combos / `Iteration==[1]`; SafeTail 1.0's `np.random.normal(0, st_dev[...])` was deleted in 2.0) — measured p99/p50 = 2.89× vs 5–50× for real tail problems. **D-41 OPEN:** ping is rank-correlated with compute speed (Spearman ρ=0.95, servers 1–4), so MinProp is near-optimal by construction. | BTP §3.7 states the correct `P(T)` and the code did not implement it — the one mechanism BTP got right (S-01). D-40+D-41 together mean the testbed cannot demonstrate what the project set out to demonstrate. | — | **YES for D-38** — changes the episodic reward, so every SafeTail-2.0 row needs a re-run. Baselines / Oracle / SafeTail 1.0 are unaffected (none uses this reward), so the comparison structure survives. |
| 2026-09-07 | followups / D-03 D-32 M-14 S-16 D-11 G5 | `plotting/plot_baselines.ipynb`, `src/controller.py`, `src/constants.py`, `src/agent.py`, `src/_manifest.py` (new), `tools/check_manifest.py` (new), `tools/make_figures.py`, `tools/tests/test_followup_fixes.py` (new) | **D-03** the `+0.005` MinProp offset deleted from *both* notebook cells (B10 — G6 stopped warning). **D-32** `access_rate_log.csv` is now a genuine per-episode series (`Controller.episode_access_rates`, reset in `finalize_episode`) instead of `mean()` over a list that was never cleared; fixed-K baselines now log access rate too, so baseline runs no longer write an empty `request_wise_access_log.csv`. **M-14** `generate_testing_plots()` implemented (per-metric testing-slice panels + `testing_metrics.csv`) **and wired** at run end — it was `pass` *and* never called. **S-16** one named `epsilon_decay_step` (subtractive, deliberately kept), `gamma_decay` demoted to an alias. **D-11** `constants.LATENCY_METRIC` was consumed nowhere; `tools/make_figures.py` now takes its default from it. **G5** `manifest.json` per run (git SHA + dirty flag, seed, full constants dump, package versions, host, wall clock, SHA-256 of every dataset CSV and model pickle, `[DEGRADED]` ledger) + `tools/check_manifest.py` enforcing it. | last unclosed items with an unambiguous disposition in plan.md §7/§B11; G5 was the only gate never started | G5 (new), G6 | N (all runs to date are unaffected; D-32 changes only how the access-rate **column** is computed, and no figure in `figures/` reads it) |
| 2026-09-03 | S-18 S-19 S-20 / retraction | `audit/ERRATA.md`, `baselines/safetail_v1/paper_v1.py` (new), `results/BASELINE_COMPARISON_REPORT.md` | SafeTail 1.0's **published code ≠ its published paper**: Bellman targets fed to a softmax+CCE head (S-18), a reward that differs from Eq. 5 in all three cases (S-19), 2 sigmoid layers instead of 5 ReLU (S-20). Added `safetail_v1_paper`, the paper-faithful variant, alongside the code-faithful `safetail_v1`. | the code-faithful port destabilises after ~decile 7; that is a property of the GitHub repo, not of the paper, and must not be attributed to the paper | — | **YES** — the headline reverses: paper-faithful 1.0 p95 68.66 ms vs 2.0's 75.66 ms |
| 2026-09-03 | D / F1–F8 | `tools/make_figures.py`, `tools/verify_figures.py`, `tools/run_matrix.py`, `tools/package_*.py`, `figures/**`, `results/**` | run matrix at 3 seeds; F1 F2 F5 F6 F7 F8 + `table_main.csv` / `table_per_seed.csv`, each with a companion CSV; 27.7× faster replay; the 3× data-budget experiment | plan.md §9 | G6 | **YES** — this is where every published number now comes from |
| 2026-09-02 | B5 / D-11 D-12 D-18 D-23 D-34 (+ LEGACY_ENV) | `src/constants.py`, `src/servers.py`, `src/controller.py`, `src/main.py`, `src/user.py` | metric semantics resolved: both `total_latency` (service) and `end_to_end_latency` logged (D-11); transmission is `f(message_size, bandwidth)` with per-type payload sizes, not a 5-value coin flip (D-12, D-34); phase 7 reuses the phase-2 draw (D-18); queue wait is simulated, not wall clock (D-23). All four are reversible via `SAFETAIL_LEGACY_ENV=1`, which reproduces `results/reference_v0` physics. | ~45% of the reported metric was policy-independent noise, and the reported deadline satisfaction was a function of how fast the test machine is | — | **YES** — changes the latency model. Legacy mode validated against `reference_v0`: transmission 20.179 vs 20.197 ms (5 distinct values both), computation 8.863 vs 8.638 ms. |
| 2026-09-02 | B9 / D-13 D-14 D-28 D-29 D-30 D-31 D-33 D-35 | `README.md`, `run_all_baselines.sh`, `src/receiver.py`, `src/controller.py`, `src/agent.py`, `src/constants.py` | README: M/M/1 section replaced with the Erlang-B loss-system reality + correction banner (D-13/D-24/D-22/S-05). `run_all_baselines.sh`: venv-relative `$SAFETAIL_PY`, K 1..β, seed loop, `results/<mode>_k<K>_s<seed>_<sha>/` layout (D-14). `receiver.py`: loopback-only bind + 8 MiB payload cap around `np.load(allow_pickle=True)` (D-31). `controller.py`: deleted broken orphan `assign_request` (3-of-7 unpack, D-28) + `dispatch_to_agent`; tagged `get_queue_lengths`/`save_checkpoint`/`export_training_data`/`generate_testing_plots` `[DEAD][D-29]`. `constants.py`: `nS`/`episode_size`/`max_load` tagged `[DEAD][D-30]`; `total_no_request` annotated with the ~15,225-not-500,000 reality (D-33) and `nS` with the variable-length-state reality (D-35). | inherited docs/scripts are false or unreachable; `np.load` of socket payloads is RCE if the port ever leaves localhost | — | N (behaviour unchanged; docs + a bind guard) |
| 2026-09-02 | C-v1 / M-02 (+M-03 ref, V1-BUG-01, V1-DEV-01/02) | `baselines/safetail_v1/{config,state,model,reward,train,policy}_v1.py` (new), `baselines/safetail_v1/README.md` (new), `tools/tests/test_c_v1_baseline.py` (new) | SafeTail 1.0 algorithm ported onto the 2.0 env through the seam: 1.0 network (sigmoid×2+BN, softmax, CCE), flat homogeneous state (`nS=2β+2`, A-1..A-4), τ-referenced 5-case reward, ε-greedy replay at the episode boundary. τ per request type from `reference_v0` medians (s 51.8ms / d 34.5ms / p 35.5ms). `SAFETAIL_V1_FREEZE_AFTER` train-then-freeze. Runs end-to-end (`--policy safetail_v1`); G4 still green. | this project's primary deliverable — the 1.0 policy driving the same servers/traces/admission as every other policy (plan.md §8) | G4 | N (new baseline; own runs) |
| 2026-09-02 | B3 / D-07 M-04 S-02 (+W-02) | `src/controller.py` (`_collapse_step_reward`), `src/constants.py` (`C_RED`, `C_RED_SWEEP`), `tools/audit_reward.py` (new), `tools/tests/test_b3_redundancy.py` (new) | step reward collapsed as `mean_{i∈A} log(1+headroom_i) − c_red·(|A|−1)/(β−1)`: mean over selected servers (not constant 6, drops the W-02 phantom slot) + explicit redundancy cost. `c_red=0` reproduces old behaviour. Gate **G3** PASS (c_red=0 flat in \|A\|; c_red=0.1 strictly decreasing). | nothing priced redundancy — all three reward terms pushed toward more servers (D-07 → over-replication D-08). S-02: BTP's headroom fix dropped HED's only \|A\|-sensitive quantities. | G3 | **pending** — the K-vs-`c_red` ablation (F8) needs full runs; smoke is too short for the agent to exploit. |
| 2026-09-02 | B2 / D-04 D-05 D-06 | `src/controller.py`, `src/agent.py`, `tools/audit_replay.py` (new) | snapshot `s_t` as an array at action time (pre-mutation); `s_{t+1}` snapshotted post-action; `DQNAgent.store_arrays`; keep per-step reward + broadcast `R_ep/N` bonus instead of overwriting. Gate **G2** PASS (450 transitions, 0 outcome-leaks, 100% `s_t≠s_{t+1}`, per-step rewards vary in 30/30 episodes). | replay state used to contain the outcome of its own action (D-04), `next_state==state` (D-05), step rewards discarded (D-06) — Bellman target was meaningless | G2 | **pending** — training signal changes; measure at next full run |
| 2026-09-02 | B1 / D-02 D-02b D-02c (+D-15, W-01) | `src/regressors.py` (new), `src/servers.py`, `src/constants.py`, `src/_legacy_regressor_wrappers/` (moved), `tools/verify_heterogeneity.py` (new), `tools/tests/test_b1_regressors.py` (new) | one `TracePredictor(server_index, task)` loading the right per-server model+CSV (ports all 3 shipped feature schemas); server 2 aliases server 1 (D-15); load/lookup failure raises unless `ALLOW_DEGRADED_PREDICTORS`. Gate **G1** PASS. | all 15 wrappers hardcoded `models/server1/` + `dataset/server1.csv` — computation path was not heterogeneous; failures were silent | G1 | **YES (expected)** — computation delay now server1/2≈12ms, server3≈388ms, server4≈266ms, server5≈13ms (was ≈12ms for all 5). Re-baseline at next run; likely moves D-10. |
| 2026-09-02 | B8 / D-16 D-17 D-19 D-20 D-21 | `src/controller.py`, `src/servers.py`, `src/user.py`, `tools/tests/test_b8_structural.py` (new), `pytest.ini` (new) | D-17 zero the failing server's own slot (was `[server_idx-1]`); D-16 drop GPU factors for CPU-only servers + geometric-mean renormalise (range stays `[0,log2]`, S-03); D-19 `Request.contention_str` — stop clobbering `.combination`, add `contention_str` column to `latency_log.csv` (`request_type` now ∈ {s,d,p}); D-20 `request_*_done` deadline-conditional at the one site `T` is known, unconditional increments removed; D-21 saturation retry is bounded (6×, exp backoff, `dropped_requests` counter) not unbounded recursion. 6/6 regression tests pass. | plan.md B8 | **pending** — reward math (D-16) and completion ratio (D-20) change; measure at next run. No run yet. |
| 2026-09-02 | B12 / S-01…S-17 | `audit/ERRATA.md` (new) | standalone specification-errata register: 17 entries, provenance code + document section + code/dataset evidence + correction per row; cross-ref table to workstreams | plan.md B12 accept: every S-row has an entry citing a section and a code location or dataset fact; advisor-facing artefact | G7 (S-14) | N (docs) |
| 2026-09-02 | B12 / S-14 | `tools/verify_types.py` (new) | gate **G7**: assert `s→Speech, d→Detect, p→Predict` from the dataset on all 5 servers, Speech slowest, `ORIGINAL_DEADLINES` pairing, and (when present) `regressors.TASK_FOR_LETTER` / legacy-wrapper `scripts.index(...)`. PASS | S-14: BTP §3.3/§4.6 prose + HED item 9 misname the types and read more authoritatively than a CSV column; G7 makes the dataset the arbiter (reverses the 19 Aug note) | — | N |
| 2026-09-02 | B7 / smoke / D-36 | `src/constants.py`, `src/_seeding.py` (new), `src/main.py`, `src/controller.py` | `--smoke` (socket-free ~20-episode run), `SEED` threaded to random/numpy/tf, `POLICY` env; UTF-8 stdout reconfigure (D-36: emoji prints crash on Windows cp1252); plots suppressed under SMOKE | every pipeline gate (G2/G3/G4a) needs a fast deterministic run; none existed | G4 | N |
| 2026-09-02 | C / M-01 / M-02 | `src/policy_registry.py` (new), `src/controller.py`, `baselines/**` | policy seam: `PolicyContext` + registry + `subset↔index`; `Controller.policy` branch in `process_step`/`finalize_episode`; `baselines/` deletable unit with oracle (M-01) running end-to-end; `tools/verify_isolation.sh` gate **G4** | plan.md §8: SafeTail 1.0 baseline is a port through one 50-line seam; `rm -rf baselines/` must leave a working repo (R3) | G4 | N |
| 2026-09-02 | B0 / D-01 | `requirements.txt` | re-encode UTF-16LE→UTF-8/LF; add `scikit-learn==1.5.2`, `scipy==1.13.1`, `seaborn==0.13.2`, `joblib`, `threadpoolctl` | a clean install per the repo's own instructions produced a system with no sklearn, so every `models/*/*.pkl` unpickle raised `ModuleNotFoundError` and D-02c fired silently on every server | G0 | N (enables real numbers; no run yet) |
| 2026-09-02 | B0 | `tools/verify_env.py` (new) | gate **G0**: assert Python 3.11/3.12, numpy 1.x, every pinned version present, all 15 regressor pickles load + expose `.predict()` | today the regressor-layer collapse is silent (D-02c); G0 makes it loud and blocks every downstream workstream | — | N |
| 2026-09-02 | — | `.git`, `CHANGELOG.md` (new) | initialise git in `heterogenous/`; seed commit of the verbatim `SafeTail-2.0-main` copy; start this changelog | checkpointing discipline (plan.md §0, §10.6) | — | N |

---

## Gate status

| Gate | Script | State |
|---|---|---|
| G0 | `tools/verify_env.py` | **PASS** (venv `.venv`, Python 3.12, tf 2.20, sklearn 1.4.2) |
| G1 | `tools/verify_heterogeneity.py` | **PASS** (computation delay now truly per-server) |
| G2 | `tools/audit_replay.py` | **PASS** (D-04/D-05/D-06 on instrumented smoke) |
| G3 | `tools/audit_reward.py` | **PASS** (redundancy priced; reward well-shaped) |
| G4 | `tools/verify_isolation.sh` | **PASS** (seam + oracle; baselines/ deletable) |
| G5 | `tools/check_manifest.py` | **PASS** (2026-09-07). Every *future* run writes `manifest.json`; the 20 run dirs that predate the gate are reported as **UNPROVENANCED**, not failures. `--strict` fails on them. |
| G6 | `tools/verify_figures.py` | **PASS** — clean since 2026-09-07 (the D-03 notebook warning is gone) |
| G7 | `tools/verify_types.py` | **PASS** (S-14 dataset mapping) |
| **G8** | `tools/verify_dataset.py` | **reports INADEQUATE** — correct: the current traces carry one averaged measurement per contention string (D-40). Re-run when the new dataset lands; `--strict` to fail CI on it. |

All eight gates are now green (G8 advisory). Regression suite: `tools/tests/` — 48 tests
(24 from session 1 + 12 follow-up), one or more per closed defect.
